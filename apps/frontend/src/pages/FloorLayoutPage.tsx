export function FloorLayoutPage() {
  return (
    <div className="p-4">
      <h2 className="text-xl font-bold">Floor Layout</h2>
      <p>Drag/drop layout canvas should persist x/y in IndexedDB and sync queue.</p>
    </div>
  );
}
